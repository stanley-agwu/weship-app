export type Delivery = {
  user?: string,
  customerName: string,
  warehouseAddress: string,
  deliveryDate: string,
  deliveryAddress: string,
}

export type User = {
  username: string,
  email: string,
  password: string,
}